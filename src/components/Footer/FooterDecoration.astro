---
// Componente para los elementos decorativos del footer
---

<div class="absolute inset-0 overflow-hidden">
  <!-- Decoración de fondo -->
  <div class="absolute inset-0 bg-gradient-to-r from-blue-50/30 via-transparent to-blue-50/30 pointer-events-none"></div>
  
  <!-- Círculos decorativos con animación sutil -->
  <div class="absolute -top-40 -right-40 w-80 h-80 rounded-full bg-blue-100/20 blur-3xl animate-float"></div>
  <div class="absolute -bottom-40 -left-20 w-60 h-60 rounded-full bg-blue-100/30 blur-3xl animate-float-delayed"></div>
  
  <!-- Elementos geométricos adicionales -->
  <div class="absolute top-20 left-1/4 w-4 h-4 bg-blue-200/10 rounded-full"></div>
  <div class="absolute bottom-32 right-1/4 w-6 h-6 bg-blue-200/20 rounded-full"></div>
  <div class="absolute top-1/3 right-1/3 w-3 h-3 bg-blue-300/10 rounded-full"></div>
</div>

<style>
  @keyframes float {
    0% { transform: translate(0, 0); }
    50% { transform: translate(-10px, 10px); }
    100% { transform: translate(0, 0); }
  }
  
  @keyframes float-delayed {
    0% { transform: translate(0, 0); }
    50% { transform: translate(10px, -10px); }
    100% { transform: translate(0, 0); }
  }
  
  .animate-float {
    animation: float 20s ease-in-out infinite;
  }
  
  .animate-float-delayed {
    animation: float-delayed 25s ease-in-out infinite;
  }
</style>
