---
/*
 * Este archivo ahora es solo un archivo de estilos globales para el menú móvil.
 * Todo el contenido ha sido migrado a componentes más pequeños en la carpeta NavbarMobileMenu/
 * para una mejor organización y mantenibilidad.
 */
---

<style is:global>
  body {
    --navbar-height: 70px;
  }
  
  /* Estilos para la transición del menú móvil */
  #mobile-menu {
    transition: max-height 0.3s ease-in-out, opacity 0.2s ease-in-out, visibility 0s 0.3s;
  }

  #mobile-menu.open {
    transition: max-height 0.5s ease-in-out, opacity 0.3s ease-in-out, visibility 0s;
  }

  /* Animaciones para elementos del menú */
  @keyframes fadeSlideIn {
    from { 
      opacity: 0;
      transform: translateY(10px);
    }
    to { 
      opacity: 1;
      transform: translateY(0);
    }
  }

  .mobile-nav-item {
    animation: fadeSlideIn 0.3s forwards;
    animation-delay: calc(var(--item-index) * 0.05s);
    opacity: 0;
  }

  /* Mejora de feedback táctil para dispositivos móviles */
  @media (hover: none) {
    .mobile-nav-item:active {
      background-color: rgba(59, 130, 246, 0.1);
    }
  }

  /* Efectos de desplazamiento y sombras */
  .navbar-scrolled {
    box-shadow: 0 4px 20px -5px rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(10px);
    background-color: rgba(255, 255, 255, 0.95);
  }

  /* Efecto de bouncing para el botón CTA */
  @keyframes pulse-bounce {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
  }

  .mobile-cta {
    animation: pulse-bounce 2s infinite;
    animation-delay: 1s;
  }

  /* Estilos para la configuración de accesibilidad */
  .focus-visible:focus-visible {
    outline: 2px solid #60a5fa;
    outline-offset: 2px;
  }
</style>